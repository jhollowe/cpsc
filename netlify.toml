[build]
# the folder where Hugo builds the site and Netlify will serve
publish = "public/"
# the build command
command = "hugo --gc --minify"

[build.environment]
HUGO_BASEURL = "https://cpsc.johnhollowell.com/"
